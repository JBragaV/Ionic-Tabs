export interface cliente{
    nome: string, //nome
    email: string, //email
    endereco: string, //endereco
    senha: string  //senha
    //INSERT INTO table_name (nome, endereco, senha, email)VALUES (a, b, c, d);
}